<Window x:Class="PlayingCards.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PlayingCards.ViewModels"
        xmlns:views="clr-namespace:PlayingCards.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:GameDeckViewModel}"
        Title="MainWindow" Height="605" Width="1005">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <views:CreatePlayerControl Grid.Row="0" Grid.Column="0" />
        <views:GameDeckControl Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" DataContext="{Binding}"/>
        <Button Grid.Row="1" Grid.Column="0" Content="Refresh" Command="{Binding RefreshCommand}"/>
        <views:GameOverViewControl Grid.Row="2" Grid.Column="0" DataContext="{Binding}"/>
    </Grid>
</Window>
